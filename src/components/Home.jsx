import React from 'react';
import logo from '../logo.png';

const Home = () => {
    return (
        <div>
            <img src={logo} style={{width:'30%'}}/>
        </div>
    );
};

export default Home;